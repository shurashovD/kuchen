import { MeasureCanvasState } from "src/types";

export const initialState: MeasureCanvasState = {
    deviceLeftFraction: 0.5, deviceTopFraction: 0.95, height: 300, rotate: Math.PI * 0.75, scale: 1, width: 300
}